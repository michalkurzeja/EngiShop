{% extends 'EngiShopBundle::layout.html.twig' %}

{% block content %}
    <h1>{{ post.title }}</h1>
    <p class="summary">{{ post.summary }}</p>
    <p>{{ post.content | raw }}</p>
    <a href="{{ path('engishop_front_post') }}">&laquo; Powrót</a>
{% endblock %}

